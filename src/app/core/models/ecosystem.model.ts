export interface Ecosystem {
  id: string;
  name: string;
  description?: string;
  logoUrl?: string;
  website?: string;
}

export interface Dataspace {
  id: string;
  name: string;
  description?: string;
  logoUrl?: string;
  website?: string;
}
